let age = 10;
let canVote = age > 18;

if (canVote) {
  console.log(`Bro you are eligible for voting is because your age is ${age}`);
} else {
  console.log(
    `Bro you are not-eligible for voting is because your age is ${age}`
  );
}
